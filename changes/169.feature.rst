This PR primarily updates phrosty to be compatible with the most recent version of snappl. It also adds docstrings in pipeline.py, additional intermediate file outputs, bug fixes (we weren't passing the sky-subtracted image to SFFT; we were passing noise instead of variance to SFFT; and applying the decorrelation kernel to the not-cross-convolved science PSF), memory tracing, and compatibility with updated SFFT.
